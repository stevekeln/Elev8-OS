Replace the listed changed files in the local Git source, commit to develop, push, run Build Elev8 OS Release.bat, and install the BAT-generated ZIP on Local. Test before installing the exact same ZIP on Live.
